public class Node {
    // A classe 'node' representa um nó na lista encadeada.
    int data; // Armazena o valor do nó
    Node next; // Armazena a referencia ao proximo nó da lista

    // O construtor inicializa o valor do nó ('data') e define 'next' como 'null'
    public Node(int data) {
        this.data = data;
        this.next = null;
    }
}





